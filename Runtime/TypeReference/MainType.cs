public static class MainType {
    public enum ConnectionType {
        DataStream,
        DataGram,
    }

    public enum personalServerSetUpFlag
    {
        defaultServer = 0,
        privateServer = 1
    }
}